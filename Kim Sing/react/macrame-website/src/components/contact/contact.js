import React from "react";
import "./contact.css";

const contact = () => (
  <div className="contact">
    <div id={"contact"}></div>
    <p className="pageTitle">Contact</p>
    contact Component
  </div>
);

export default contact;
